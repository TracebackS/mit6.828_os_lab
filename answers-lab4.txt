516030910459 邵欣阳

Question
1. 在打开AP的保护模式之前，无法寻址1MB以上的空间，所以使用MPBOOTPHYS计算相应的物理地址。
   在boot.S中，尚未启用分页机制，可以直接制定程序入口。而mpentry.S中，不能直接指定物理地址，但是可以将线性地址映射到物理地址。

2. 因为不同的cpu要为自己维护不同的栈内容，而中断的存在会导致尚未加锁就被另一个cpu访问栈，使用同一个栈会导致不同cpu的内核信息混乱。
   出错场景：cpu0压栈，尚未加锁，收到中断，cpu1，并将它的信息push到栈上，此时栈已经混乱了。

3. 因为页表对内核地址空间的映射完全相同，所以无论怎样，对e的地址翻译的结果都是一样的。

4. 因为不保存信息，就无法恢复这个环境。当用户进程调用系统函数时，会发生。

Challenge
没有做challenge，知难而退。
